// PRIMITIVE
// In JavaScript, a primitive (primitive value, primitive data type)
// is data that is not an object and has no methods or properties.


// String
const name = 'Javid';

// Number 
const age = 25;

// Boolean
const hasKids = true;

// Null
const cycle = null;

// Undefined
let test;

// Symbol
const sym = Symbol();

// console.log(name,age,hasKids,cycle,test,sym);

// REFERENCE TYPES - objects

// Array
const hobbies = ["movies", "music"];

// Object literal

const address = {
    city: "Chennai",
    state: "TN"
}

const today = new Date();

console.log(today);
console.log(typeof today);